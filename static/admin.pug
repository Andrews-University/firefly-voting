doctype html
html(xmlns="http://www.w3.org/1999/xhtml" lang="en")
	head
		meta(charset="utf-8")
		meta(name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, user-scalable=no")
		title Firefly Admin
		link(rel="shortcut icon" type="image/png" href="img/favicon.png")
		script(type="application/javascript" src="admin.js" defer)
		link(rel="stylesheet" type="text/css" href="css/bootstrap.css")
		style(type="text/css").
			body {
				font: 13px Helvetica, Arial;
				display: flex;
				flex-direction: column;
			}
			.firefly-bar {
				background: rgba(0, 64, 111, 0.85);
				padding: 8px;
				width: 100%;
				flex-shrink: 0;
				display: flex;
				justify-content: space-between;
				flex-wrap: wrap;
				position: fixed;
				height: 400px;
			}
			.firefly-bar input {
				border: 0;
				padding: 10px;
				width: 90%;
				margin-right: .5%;
			}
			.firefly-bar button {
				width: 49%;
				background: #be9549;
				border: none;
				padding: 10px;
				margin: 12px 0;
				min-height: 100px;
				font-size: 24px;
				color: white;
			}
			#reset {
				margin: 12px auto;
				background-color: rgba(0, 64, 111, 1);
			}
			#messages {
				list-style-type: none;
				margin-top: 400px;
				padding: 0;
				flex: 1 0 auto;
			}
			#messages > li {
				padding: 5px 10px;
			}
			#messages > li > time:first-child {
				padding-right: 2em;
				font-size: 0.7em;
			}
			#messages > li:nth-child(odd) {
				background: #bbb;
			}
	body
		.firefly-bar
			button#close(type="button") Close Voting
			button#open(type="button") Open Voting
			button#prev(type="button") ⟵ Close voting, previous category
			button#next(type="button") Close voting, next category ⟶
			button#reset(type="button") Reset the vote count for the current category
		ul#messages
